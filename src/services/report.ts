import {query} from '../db'; export function revenueByMonth(y:number,m:number){const ym=`${y}-${String(m).padStart(2,'0')}`; const r=query<{total:number}>(`SELECT COALESCE(SUM(total),0) AS total FROM invoices WHERE substr(period_start,1,7)=?`,[ym]); return r[0]?.total??0;}